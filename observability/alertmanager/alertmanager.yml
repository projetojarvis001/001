global:
  resolve_timeout: 5m

route:
  group_by: ['alertname']
  group_wait: 10s
  group_interval: 10s
  repeat_interval: 1h
  receiver: telegram

receivers:
  - name: telegram
    webhook_configs:
      - url: 'http://192.168.8.121:3000/alerts/webhook'
        send_resolved: true
