<!DOCTYPE html>
<html lang="pt-BR">
<head>
<meta charset="UTF-8">
<meta name="viewport" content="width=device-width,initial-scale=1,viewport-fit=cover">
<meta http-equiv="refresh" content="30">
<title>JARVIS — Command Center</title>
<link rel="preconnect" href="https://fonts.googleapis.com">
<link href="https://fonts.googleapis.com/css2?family=Space+Mono:wght@400;700&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
<style>
:root{
--bg:#080b12;--surface:#0d1117;--surface2:#131920;--border:#1e2d3d;
--green:#00ff88;--green-dim:#00ff8822;--amber:#ffb800;--amber-dim:#ffb80022;
--red:#ff3b5c;--red-dim:#ff3b5c22;--blue:#00aaff;--blue-dim:#00aaff22;
--text:#c9d8e8;--muted:#4a6070;--mono:'Space Mono',monospace;--sans:'DM Sans',sans-serif;
}
*{margin:0;padding:0;box-sizing:border-box;-webkit-tap-highlight-color:transparent}
html{background:var(--bg);color:var(--text);font-family:var(--sans);font-size:14px;line-height:1.5}
body{min-height:100vh;padding:env(safe-area-inset-top) env(safe-area-inset-right) env(safe-area-inset-bottom) env(safe-area-inset-left)}

.topbar{display:flex;align-items:center;gap:10px;padding:12px 16px;background:var(--surface);border-bottom:1px solid var(--border);position:sticky;top:0;z-index:100;backdrop-filter:blur(12px)}
.topbar-logo{font-family:var(--mono);font-size:13px;font-weight:700;color:var(--green);letter-spacing:.1em}
.topbar-sub{font-size:11px;color:var(--muted);display:none}
@media(min-width:480px){.topbar-sub{display:block}}
.topbar-ts{font-family:var(--mono);font-size:10px;color:var(--muted);margin-left:auto}
.topbar-score{display:flex;align-items:center;gap:6px;padding:4px 10px;border-radius:6px;font-family:var(--mono);font-size:12px;font-weight:700;border:1px solid;margin-left:8px}

.main{padding:12px;max-width:1400px;margin:0 auto}
@media(min-width:768px){.main{padding:16px 20px}}

.alert-bar{display:none;background:var(--red-dim);border:1px solid var(--red);border-radius:8px;padding:10px 14px;margin-bottom:12px;font-size:12px;color:var(--red)}
.alert-bar.warn{background:var(--amber-dim);border-color:var(--amber);color:var(--amber)}
.alert-bar.active{display:block}

.score-hero{background:var(--surface);border:1px solid var(--border);border-radius:12px;padding:16px;margin-bottom:12px;display:grid;grid-template-columns:auto 1fr;gap:16px;align-items:center}
.score-big{font-family:var(--mono);font-size:56px;font-weight:700;line-height:1;min-width:80px}
@media(min-width:480px){.score-big{font-size:72px;min-width:100px}}
.score-label{font-size:11px;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin-bottom:4px}
.score-status{font-size:18px;font-weight:600;margin-bottom:8px}
.score-track{height:6px;background:var(--border);border-radius:3px;overflow:hidden}
.score-fill{height:100%;border-radius:3px;transition:width .8s cubic-bezier(.4,0,.2,1)}
.score-meta{display:flex;gap:16px;margin-top:8px;flex-wrap:wrap}
.score-meta-item{font-size:11px;color:var(--muted)}
.score-meta-item span{color:var(--text);font-weight:500}

.kpi-grid{display:grid;grid-template-columns:repeat(2,1fr);gap:8px;margin-bottom:12px}
@media(min-width:480px){.kpi-grid{grid-template-columns:repeat(3,1fr)}}
@media(min-width:900px){.kpi-grid{grid-template-columns:repeat(6,1fr)}}
.kpi{background:var(--surface);border:1px solid var(--border);border-radius:10px;padding:12px;position:relative;overflow:hidden}
.kpi::before{content:'';position:absolute;top:0;left:0;right:0;height:2px}
.kpi.ok::before{background:var(--green)}
.kpi.warn::before{background:var(--amber)}
.kpi.fail::before{background:var(--red)}
.kpi.info::before{background:var(--blue)}
.kpi-label{font-size:9px;color:var(--muted);letter-spacing:.08em;text-transform:uppercase;margin-bottom:4px}
.kpi-val{font-family:var(--mono);font-size:20px;font-weight:700}
@media(min-width:480px){.kpi-val{font-size:24px}}
.kpi-sub{font-size:10px;color:var(--muted);margin-top:2px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}

.section-label{font-size:10px;color:var(--muted);letter-spacing:.1em;text-transform:uppercase;margin:16px 0 8px;padding-left:2px;font-weight:600}

.layer{background:var(--surface);border:1px solid var(--border);border-radius:10px;margin-bottom:8px;overflow:hidden}
.layer-header{padding:10px 14px;border-bottom:1px solid var(--border);display:flex;align-items:center;gap:8px;cursor:pointer;user-select:none}
.layer-num{font-family:var(--mono);font-size:10px;color:var(--muted);background:var(--surface2);padding:2px 7px;border-radius:4px}
.layer-title{font-size:12px;font-weight:600;flex:1}
.layer-badge{font-size:10px;padding:2px 8px;border-radius:4px;font-family:var(--mono)}
.layer-body{padding:10px 14px}

.svc-row{display:flex;align-items:center;gap:8px;padding:7px 0;border-bottom:1px solid var(--border)}
.svc-row:last-child{border-bottom:none}
.dot{width:7px;height:7px;border-radius:50%;flex-shrink:0;position:relative}
.dot.ok{background:var(--green);box-shadow:0 0 6px var(--green)}
.dot.warn{background:var(--amber);box-shadow:0 0 6px var(--amber)}
.dot.fail{background:var(--red);box-shadow:0 0 6px var(--red);animation:blink 1s infinite}
.dot.dim{background:var(--muted)}
@keyframes blink{0%,100%{opacity:1}50%{opacity:.3}}
.svc-name{flex:1;font-size:12px;color:var(--text)}
.svc-val{font-family:var(--mono);font-size:10px;color:var(--muted);text-align:right;max-width:120px;white-space:nowrap;overflow:hidden;text-overflow:ellipsis}
.svc-badge{font-size:9px;padding:2px 7px;border-radius:4px;font-weight:600;margin-left:6px;white-space:nowrap}
.sb-ok{background:var(--green-dim);color:var(--green)}
.sb-warn{background:var(--amber-dim);color:var(--amber)}
.sb-fail{background:var(--red-dim);color:var(--red)}
.sb-dim{background:var(--surface2);color:var(--muted)}

.net-grid{display:grid;grid-template-columns:1fr 1fr;gap:8px;margin-top:8px}
@media(min-width:600px){.net-grid{grid-template-columns:repeat(4,1fr)}}
.net-card{background:var(--surface2);border-radius:8px;padding:10px;text-align:center}
.net-val{font-family:var(--mono);font-size:18px;font-weight:700;margin:2px 0}
.net-label{font-size:9px;color:var(--muted);letter-spacing:.06em;text-transform:uppercase}

.issues-box{background:var(--red-dim);border:1px solid var(--red);border-radius:8px;padding:10px 14px;margin-bottom:6px}
.warns-box{background:var(--amber-dim);border:1px solid var(--amber);border-radius:8px;padding:10px 14px}
.issue-item,.warn-item{font-size:11px;padding:2px 0;font-family:var(--mono)}
.issue-item{color:var(--red)}
.warn-item{color:var(--amber)}

.schedule-row{display:flex;align-items:center;gap:8px;padding:6px 0;border-bottom:1px solid var(--border)}
.schedule-row:last-child{border-bottom:none}
.sched-time{font-family:var(--mono);font-size:11px;color:var(--green);min-width:48px}
.sched-name{font-size:12px;flex:1}
.sched-node{font-size:10px;color:var(--muted)}

.footer{text-align:center;padding:16px;font-family:var(--mono);font-size:9px;color:var(--muted);letter-spacing:.1em}
</style>
</head>
<body>

<div class="topbar">
  <div class="topbar-logo">J.A.R.V.I.S.</div>
  <div class="topbar-sub">Grupo Wagner · Mac1 192.168.8.121 · Mac2 192.168.8.124</div>
  <div class="topbar-ts" id="ts">--:--</div>
  <div class="topbar-score" id="top-score" style="color:var(--green);border-color:var(--green)">-- /100</div>
</div>

<div class="main">

  <div class="alert-bar" id="alert-bar"></div>

  <div class="score-hero">
    <div class="score-big" id="score-big" style="color:var(--green)">--</div>
    <div>
      <div class="score-label">Health Score Global</div>
      <div class="score-status" id="score-status">Carregando...</div>
      <div class="score-track"><div class="score-fill" id="score-fill" style="width:0;background:var(--green)"></div></div>
      <div class="score-meta">
        <div class="score-meta-item">RAM <span id="m-ram">--</span>MB</div>
        <div class="score-meta-item">Disco <span id="m-disk">--</span>%</div>
        <div class="score-meta-item">Latência <span id="m-lat">--</span>ms</div>
        <div class="score-meta-item">IP <span id="m-ip">--</span></div>
        <div class="score-meta-item">ISP <span id="m-isp">--</span></div>
      </div>
    </div>
  </div>

  <div class="kpi-grid">
    <div class="kpi ok" id="kpi-core"><div class="kpi-label">Core</div><div class="kpi-val" id="kv-core">--</div><div class="kpi-sub">jarvis-core :3000</div></div>
    <div class="kpi ok" id="kpi-vision"><div class="kpi-label">VISION</div><div class="kpi-val" id="kv-vision">--</div><div class="kpi-sub">semantic :5006</div></div>
    <div class="kpi ok" id="kpi-pg"><div class="kpi-label">Postgres</div><div class="kpi-val" id="kv-pg">--</div><div class="kpi-sub">:5432 jarvis_db</div></div>
    <div class="kpi ok" id="kpi-redis"><div class="kpi-label">Redis</div><div class="kpi-val" id="kv-redis">--</div><div class="kpi-sub">:6379 cache</div></div>
    <div class="kpi info" id="kpi-vec"><div class="kpi-label">Vetores</div><div class="kpi-val ok" style="color:var(--blue)">146</div><div class="kpi-sub">Grupo Wagner</div></div>
    <div class="kpi ok" id="kpi-n8n"><div class="kpi-label">N8n</div><div class="kpi-val" id="kv-n8n">--</div><div class="kpi-sub">orquestração :5678</div></div>
  </div>

  <div class="section-label">Camada 1 — Fundação</div>
  <div class="layer">
    <div class="layer-body">
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Hardware Mac1</span><span class="svc-val" id="hw-mac1">M4 · 16GB · 256GB</span><span class="svc-badge sb-ok">OK</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Hardware Mac2</span><span class="svc-val">M4 · 16GB · 256GB</span><span class="svc-badge sb-ok">OK</span></div>
      <div class="svc-row"><div class="dot" id="d-internet"></div><span class="svc-name">Internet</span><span class="svc-val" id="v-internet">--</span><span class="svc-badge" id="b-internet">--</span></div>
      <div class="svc-row"><div class="dot" id="d-tunnel"></div><span class="svc-name">Cloudflare Tunnel</span><span class="svc-val" id="v-tunnel" style="font-size:9px">--</span><span class="svc-badge" id="b-tunnel">--</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Link 1 — Net Turbo</span><span class="svc-val" id="link1-ip">186.209.45.40 · Campinas</span><span class="svc-badge sb-ok">ATIVO</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Link 2 — Elevar Telecom</span><span class="svc-val" id="link2-ip">45.170.99.120 · Campinas</span><span class="svc-badge sb-ok" id="link2-badge">ATIVO</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Rede local</span><span class="svc-val">192.168.8.0/24</span><span class="svc-badge sb-ok">LAN OK</span></div>
      <div class="net-grid">
        <div class="net-card"><div class="net-label">CF Latência</div><div class="net-val ok" id="n-cf">--</div><div class="net-label">ms</div></div>
        <div class="net-card"><div class="net-label">Telegram</div><div class="net-val" id="n-tg">--</div><div class="net-label">ms</div></div>
        <div class="net-card"><div class="net-label">Banda</div><div class="net-val info" id="n-bw">--</div><div class="net-label">KB/s</div></div>
        <div class="net-card"><div class="net-label">Conn externas</div><div class="net-val" id="n-conn">--</div><div class="net-label">ativas</div></div>
      </div>
    </div>
  </div>

  <div class="section-label">Camada 2 — Plataforma</div>
  <div class="layer">
    <div class="layer-body">
      <div class="svc-row"><div class="dot" id="d-core"></div><span class="svc-name">jarvis-core</span><span class="svc-val">Node.js · Express · :3000</span><span class="svc-badge" id="b-core">--</span></div>
      <div class="svc-row"><div class="dot" id="d-vision"></div><span class="svc-name">VISION Semantic API</span><span class="svc-val">FastAPI · Python · :5006</span><span class="svc-badge" id="b-vision">--</span></div>
      <div class="svc-row"><div class="dot" id="d-pg"></div><span class="svc-name">PostgreSQL</span><span class="svc-val">v16-alpine · :5432</span><span class="svc-badge" id="b-pg">--</span></div>
      <div class="svc-row"><div class="dot" id="d-redis"></div><span class="svc-name">Redis</span><span class="svc-val">v7-alpine · :6379</span><span class="svc-badge" id="b-redis">--</span></div>
      <div class="svc-row"><div class="dot" id="d-n8n"></div><span class="svc-name">N8n</span><span class="svc-val">orquestração visual · :5678</span><span class="svc-badge" id="b-n8n">--</span></div>
      <div class="svc-row"><div class="dot" id="d-ollama"></div><span class="svc-name">Ollama Mac2</span><span class="svc-val">gemma3:12b · qwen2.5:14b · llama3.1:8b</span><span class="svc-badge" id="b-ollama">--</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Grafana</span><span class="svc-val">observabilidade · :3001</span><span class="svc-badge sb-dim">MONITORING</span></div>
    </div>
  </div>

  <div class="section-label">Camada 3 — Sistema</div>
  <div class="layer">
    <div class="layer-body">
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Dispatcher + Cost Router</span><span class="svc-val">Groq→Gemini→Deepseek→Ollama</span><span class="svc-badge sb-ok">AUTO</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">ZeroClaw Gateway</span><span class="svc-val">Llama4 Scout · :42617</span><span class="svc-badge sb-ok">ON</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Sentinel Agent</span><span class="svc-val">RAM · CPU · VISION · 30s</span><span class="svc-badge sb-ok">ACTIVE</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Guardian Mac1</span><span class="svc-val">auto-fix · 2min</span><span class="svc-badge sb-ok">ON</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Guardian Mac2</span><span class="svc-val">auto-fix · 2min</span><span class="svc-badge sb-ok">ON</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Memory Optimizer</span><span class="svc-val">Mac1 + Mac2 · 5min</span><span class="svc-badge sb-ok">ON</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Dev Agent</span><span class="svc-val">auto-fix código · 1h</span><span class="svc-badge sb-ok">ON</span></div>
    </div>
  </div>

  <div class="section-label">Camada 4 — Operação</div>
  <div class="layer">
    <div class="layer-body">
      <div class="svc-row"><div class="dot" id="d-telegram"></div><span class="svc-name">Telegram Bot</span><span class="svc-val">@hubOSTelegrambot · polling</span><span class="svc-badge" id="b-telegram">--</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Gmail Pipeline</span><span class="svc-val">projetojarvis001@gmail.com</span><span class="svc-badge sb-ok">OK</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Health Agent</span><span class="svc-val">5min · score → dashboard</span><span class="svc-badge sb-ok">ON</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Network Agent</span><span class="svc-val">5min · IP · latência · banda</span><span class="svc-badge sb-ok">ON</span></div>
      <div style="margin-top:10px">
        <div class="schedule-row"><span class="sched-time">06:50</span><span class="sched-name">Health Report VISION</span><span class="sched-node">Mac2</span></div>
        <div class="schedule-row"><span class="sched-time">07:00</span><span class="sched-name">Briefing Matinal JARVIS</span><span class="sched-node">Mac1</span></div>
        <div class="schedule-row"><span class="sched-time">08:00</span><span class="sched-name">Leitor do Futuro</span><span class="sched-node">Mac1</span></div>
        <div class="schedule-row"><span class="sched-time">08:30</span><span class="sched-name">Bitcoin Hunter</span><span class="sched-node">Mac1</span></div>
        <div class="schedule-row"><span class="sched-time">09:00</span><span class="sched-name">N8n Hunter WPS Digital</span><span class="sched-node">Mac1 seg-sex</span></div>
        <div class="schedule-row"><span class="sched-time">10:00</span><span class="sched-name">Pipeline Vendas Autônomo</span><span class="sched-node">Mac1</span></div>
      </div>
    </div>
  </div>

  <div class="section-label">Camada 5 — Inteligência</div>
  <div class="layer">
    <div class="layer-body">
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Base Vetorial VISION</span><span class="svc-val">146 vetores · Grupo Wagner</span><span class="svc-badge sb-ok">ATIVA</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Gemma3:12b</span><span class="svc-val">8.1GB · Mac2 · multimodal</span><span class="svc-badge sb-ok">LOADED</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Qwen2.5:14b</span><span class="svc-val">9.0GB · Mac2 · reasoning</span><span class="svc-badge sb-ok">LOADED</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Llama3.1:8b</span><span class="svc-val">4.9GB · Mac2 · fast</span><span class="svc-badge sb-ok">LOADED</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Llama4 Scout (ZeroClaw)</span><span class="svc-val">Groq cloud · web search</span><span class="svc-badge sb-ok">ONLINE</span></div>
      <div class="svc-row"><div class="dot ok"></div><span class="svc-name">Memória Episódica</span><span class="svc-val">PostgreSQL · session_facts</span><span class="svc-badge sb-ok">ATIVA</span></div>
    </div>
  </div>

  <div id="issues-section" style="display:none;margin-top:8px">
    <div class="section-label" style="color:var(--red)">Problemas ativos</div>
    <div class="issues-box" id="issues-list"></div>
  </div>
  <div id="warns-section" style="display:none;margin-top:8px">
    <div class="section-label" style="color:var(--amber)">Avisos</div>
    <div class="warns-box" id="warns-list"></div>
  </div>

</div>

<div class="footer">JARVIS v2.0 · GRUPO WAGNER · MISSÃO DADA É MISSÃO CUMPRIDA · <span id="ft-ts"></span></div>

<script>
const $ = id => document.getElementById(id);
const now = new Date();
$('ts').textContent = now.toLocaleTimeString('pt-BR',{hour:'2-digit',minute:'2-digit'});
$('ft-ts').textContent = now.toLocaleString('pt-BR');

function dotClass(ok){ return ok ? 'dot ok' : 'dot fail'; }
function badgeClass(ok){ return ok ? 'svc-badge sb-ok' : 'svc-badge sb-fail'; }
function badgeText(ok, okT, failT){ return ok ? okT : failT; }

function applyScore(s){
  const color = s>=90?'var(--green)':s>=70?'var(--amber)':s>=50?'#ff8800':'var(--red)';
  const status = s>=90?'🟢 SAUDÁVEL':s>=70?'🟡 ATENÇÃO':s>=50?'🟠 DEGRADADO':'🔴 CRÍTICO';
  $('score-big').textContent = s;
  $('score-big').style.color = color;
  $('score-status').textContent = status;
  $('score-fill').style.width = s+'%';
  $('score-fill').style.background = color;
  $('top-score').textContent = s+'/100';
  $('top-score').style.color = color;
  $('top-score').style.borderColor = color;

  if(s < 70){
    const ab = $('alert-bar');
    ab.className = 'alert-bar ' + (s<50?'active':'warn active');
    ab.textContent = s<50 ? '🔴 SISTEMA DEGRADADO — Intervenção necessária' : '⚠️ SISTEMA COM ATENÇÃO — Verificar avisos abaixo';
  }
}

function kpiStatus(kpiId, valId, ok, val){
  const kpi = $(kpiId);
  if(kpi){ kpi.className = 'kpi '+(ok?'ok':'fail'); }
  if($(valId)){ $(valId).textContent = ok ? val : 'FAIL'; $(valId).style.color = ok?'var(--green)':'var(--red)'; }
}

fetch('health_report.json?t='+Date.now()).then(r=>r.json()).then(d=>{
  applyScore(d.score);
  $('m-ram').textContent = d.mem_mb;
  $('m-disk').textContent = d.disk_pct;
  $('m-ip').textContent = d.public_ip || '--';
  $('hw-mac1').textContent = 'M4 · '+d.mem_mb+'MB livre · '+d.disk_pct+'% disco';

  const cOk = d.core==='200';
  const vOk = d.vision==='200';
  const pgOk = d.postgres==='1';
  const rdOk = d.redis==='1';
  const n8Ok = d.n8n==='200';
  const tgOk = d.telegram==='1';
  const tnOk = !!d.tunnel && d.tunnel.length > 5;

  kpiStatus('kpi-core','kv-core', cOk, '200');
  kpiStatus('kpi-vision','kv-vision', vOk, '200');
  kpiStatus('kpi-pg','kv-pg', pgOk, 'OK');
  kpiStatus('kpi-redis','kv-redis', rdOk, 'OK');
  kpiStatus('kpi-n8n','kv-n8n', n8Ok, '200');

  $('d-core').className = dotClass(cOk);
  $('b-core').className = badgeClass(cOk); $('b-core').textContent = cOk?'HTTP 200':'FAIL';
  $('d-vision').className = dotClass(vOk);
  $('b-vision').className = badgeClass(vOk); $('b-vision').textContent = vOk?'HTTP 200':'FAIL';
  $('d-pg').className = dotClass(pgOk);
  $('b-pg').className = badgeClass(pgOk); $('b-pg').textContent = pgOk?'HEALTHY':'FAIL';
  $('d-redis').className = dotClass(rdOk);
  $('b-redis').className = badgeClass(rdOk); $('b-redis').textContent = rdOk?'PONG':'FAIL';
  $('d-n8n').className = dotClass(n8Ok);
  $('b-n8n').className = badgeClass(n8Ok); $('b-n8n').textContent = n8Ok?'HTTP 200':'OFFLINE';
  $('d-telegram').className = dotClass(tgOk);
  $('b-telegram').className = badgeClass(tgOk); $('b-telegram').textContent = tgOk?'ONLINE':'FAIL';
  $('d-tunnel').className = dotClass(tnOk);
  $('b-tunnel').className = badgeClass(tnOk); $('b-tunnel').textContent = tnOk?'ATIVO':'OFFLINE';
  $('v-tunnel').textContent = tnOk ? d.tunnel.replace('https://','') : 'offline';

  const issues = d.issues.split('|').filter(x=>x.trim());
  const warns = d.warnings.split('|').filter(x=>x.trim());
  if(issues.length){
    $('issues-section').style.display='block';
    $('issues-list').innerHTML = issues.map(i=>`<div class="issue-item">${i}</div>`).join('');
  }
  if(warns.length){
    $('warns-section').style.display='block';
    $('warns-list').innerHTML = warns.map(w=>`<div class="warn-item">${w}</div>`).join('');
  }
}).catch(e=>{ applyScore(0); });

fetch('network_report.json?t='+Date.now()).then(r=>r.json()).then(d=>{
  $('m-ip').textContent = d.public_ip;
  $('m-isp').textContent = d.isp || '--';
  $('m-lat').textContent = d.latency_cf_ms;
  $('n-cf').textContent = d.latency_cf_ms;
  $('n-cf').style.color = d.latency_cf_ms<50?'var(--green)':d.latency_cf_ms<150?'var(--amber)':'var(--red)';
  $('n-tg').textContent = d.latency_telegram_ms || '--';
  $('n-bw').textContent = d.bandwidth_kb;
  $('n-conn').textContent = d.conn_external;
  const netOk = d.internet==='200'||d.internet==='301'||d.internet==='302';
  $('d-internet').className = dotClass(netOk);
  $('b-internet').className = badgeClass(netOk);
  $('b-internet').textContent = netOk?'ONLINE':'OFFLINE';
  $('v-internet').textContent = d.public_ip+' · '+d.isp;
}).catch(()=>{});

fetch('http://192.168.8.124:5006/health').then(r=>r.json()).then(d=>{
  $('d-ollama').className = 'dot ok';
  $('b-ollama').className = 'svc-badge sb-ok';
  $('b-ollama').textContent = '5 MODELOS';
}).catch(()=>{
  $('d-ollama').className = 'dot fail';
  $('b-ollama').className = 'svc-badge sb-fail';
  $('b-ollama').textContent = 'OFFLINE';
});
</script>
</body>
</html>
